using ClrScope.Mcp.Tools.Artifacts;
using Xunit;

namespace ClrScope.Mcp.Tests.Infrastructure.Tools.Artifacts;

public class ArtifactToolsTests
{
    [Fact]
    public void ValidateArtifactPath_DoesNotThrow_WhenPathIsWithinRoot()
    {
        // This is a private method, so we can't test it directly
        // We'll test the public methods that use it instead
        // For now, this is a placeholder to indicate where ArtifactTools tests should go
    }
}
